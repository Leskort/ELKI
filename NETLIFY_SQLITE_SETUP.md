# 🚀 Деплой на Netlify с SQLite (без PostgreSQL)

## 📋 Варианты хранения SQLite на Netlify

Netlify - это serverless платформа, которая не сохраняет файлы между запросами. Поэтому нужно использовать внешнее хранилище для SQLite файла.

### Вариант 1: Cloudflare R2 (Рекомендуется) ✅

**Преимущества:**
- Бесплатный тариф (10 GB хранилища, 1M запросов/месяц)
- S3-совместимый API
- Быстрый и надежный

**Настройка:**

1. **Создайте аккаунт Cloudflare R2:**
   - Зайдите на https://dash.cloudflare.com
   - R2 → Create bucket
   - Назовите bucket (например, `elki-db`)

2. **Настройте переменные окружения в Netlify:**
   ```
   DATABASE_URL=file:./dev.db
   R2_ACCOUNT_ID=ваш-account-id
   R2_ACCESS_KEY_ID=ваш-access-key
   R2_SECRET_ACCESS_KEY=ваш-secret-key
   R2_BUCKET_NAME=elki-db
   R2_PUBLIC_URL=https://ваш-bucket.r2.dev
   ```

3. **Создайте Netlify Function для синхронизации БД:**
   - Создайте файл `netlify/functions/sync-db.ts`
   - Эта функция будет загружать/скачивать БД из R2

### Вариант 2: JSON файлы в репозитории (Простой, но ограниченный) ⚠️

**Преимущества:**
- Очень просто
- Не требует дополнительных сервисов

**Недостатки:**
- Медленно при большом количестве данных
- Нужно коммитить изменения в Git
- Не подходит для частых обновлений

**Настройка:**

1. Используйте JSON файлы вместо базы данных
2. Храните данные в `data/products.json`, `data/categories.json`
3. Читайте/пишите через файловую систему

### Вариант 3: Turso (SQLite в облаке) 🌟

**Преимущества:**
- Специально для SQLite
- Бесплатный тариф (500 MB, 500M rows)
- Очень простой в использовании

**Настройка:**

1. **Создайте аккаунт на Turso:**
   - Зайдите на https://turso.tech
   - Создайте базу данных
   - Скопируйте connection string

2. **Обновите Prisma schema:**
   ```prisma
   datasource db {
     provider = "libsql"
     url      = env("DATABASE_URL")
   }
   ```

3. **Установите драйвер:**
   ```bash
   npm install @libsql/client
   ```

4. **Настройте переменные окружения:**
   ```
   DATABASE_URL=libsql://ваш-connection-string
   ```

## 🎯 Рекомендация: Используйте Turso

Turso - это самый простой вариант для SQLite на serverless платформах. Он специально создан для этого случая.

### Быстрая настройка Turso:

1. Зайдите на https://turso.tech
2. Создайте бесплатный аккаунт
3. Создайте базу данных
4. Скопируйте connection string
5. Добавьте в Netlify переменные окружения:
   ```
   DATABASE_URL=libsql://ваш-connection-string
   ```

Всё! Turso работает как SQLite, но в облаке.

## 📝 Что нужно сделать сейчас:

1. Выберите вариант (рекомендую Turso)
2. Настройте хранилище
3. Обновите `DATABASE_URL` в Netlify
4. Выполните миграции

