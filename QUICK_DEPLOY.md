# ⚡ Быстрый старт для Netlify

## 🎯 Минимум действий:

1. **Создайте PostgreSQL БД** на https://neon.tech (бесплатно)
2. **Создайте репозиторий на GitHub** и загрузите код
3. **Подключите к Netlify** через GitHub
4. **Добавьте 3 переменные окружения:**
   - `DATABASE_URL` (из Neon)
   - `NEXTAUTH_SECRET` (случайная строка 32+ символов)
   - `NEXTAUTH_URL` (ваш URL на Netlify)
5. **Выполните миграции** после первого деплоя

## 📝 Команды для GitHub:

```bash
# Создайте репозиторий на GitHub, затем:
git remote add origin https://github.com/ВАШ-USERNAME/elki.git
git branch -M main
git push -u origin main
```

## 🔑 Переменные окружения Netlify:

```
DATABASE_URL=postgresql://...
NEXTAUTH_SECRET=ваш-секрет-32-символа
NEXTAUTH_URL=https://ваш-сайт.netlify.app
```

## ✅ Готово!

После деплоя выполните миграции (см. NETLIFY_SETUP.md)

