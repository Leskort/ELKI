# 🔧 Решение проблем с отображением на Netlify

## Проблема: Сайт не выглядит правильно после деплоя

### ✅ Что уже исправлено:

1. **Стили CSS** - добавлены `!important` флаги для принудительного применения
2. **Ширина контейнеров** - добавлен `w-full` ко всем секциям
3. **Overflow** - добавлен `overflow-x-hidden` для предотвращения горизонтальной прокрутки
4. **Обработка ошибок API** - улучшена обработка ошибок при загрузке данных

### 🔍 Диагностика проблемы:

#### 1. Проверьте консоль браузера

Откройте DevTools (F12) и проверьте:
- **Console** - есть ли ошибки JavaScript?
- **Network** - загружаются ли CSS файлы? (должны быть файлы `.css`)
- **Network** - работают ли API запросы? (`/api/products`, `/api/categories`)

#### 2. Проверьте переменные окружения в Netlify

1. Зайдите в **Site settings** → **Environment variables**
2. Убедитесь, что все переменные добавлены:
   ```
   DATABASE_URL=libsql://elki-db-leskort.aws-us-east-1.turso.io?authToken=ВАШ-ТОКЕН
   NEXTAUTH_SECRET=ваш-секрет
   NEXTAUTH_URL=https://elkis.netlify.app
   ```

#### 3. Проверьте, что база данных инициализирована

**Проблема:** Если база данных не инициализирована, API будет возвращать ошибки.

**Решение:** Выполните миграции локально:

```bash
# Замените ВАШ-ТОКЕН на реальный токен из Turso
DATABASE_URL="libsql://elki-db-leskort.aws-us-east-1.turso.io?authToken=ВАШ-ТОКЕН" npm run db:migrate
DATABASE_URL="libsql://elki-db-leskort.aws-us-east-1.turso.io?authToken=ВАШ-ТОКЕН" npm run db:seed
```

#### 4. Очистите кэш браузера

- **Chrome/Edge:** `Ctrl + Shift + Delete` → выберите "Кэшированные изображения и файлы"
- Или откройте сайт в режиме инкогнито (`Ctrl + Shift + N`)

#### 5. Пересоберите сайт на Netlify

1. Зайдите в **Deploys**
2. Нажмите **"Trigger deploy"** → **"Clear cache and deploy site"**
3. Дождитесь завершения сборки

### 🚨 Частые проблемы:

#### Проблема 1: Белый экран / контент не загружается

**Причина:** База данных не инициализирована или API возвращает ошибки

**Решение:**
1. Выполните миграции (см. выше)
2. Проверьте логи Netlify: **Site settings** → **Functions** → **Logs**
3. Проверьте, что `DATABASE_URL` правильно настроен

#### Проблема 2: Стили не применяются

**Причина:** Tailwind CSS не скомпилировался или кэш браузера

**Решение:**
1. Очистите кэш браузера
2. Пересоберите сайт на Netlify с очисткой кэша
3. Проверьте, что файл `tailwind.config.js` содержит `safelist` (уже добавлен)

#### Проблема 3: Только часть контента видна

**Причина:** Проблемы с шириной контейнеров или overflow

**Решение:** Уже исправлено в последнем коммите. Убедитесь, что изменения задеплоены.

### 📝 Проверочный список:

- [ ] Переменные окружения добавлены в Netlify
- [ ] База данных инициализирована (миграции выполнены)
- [ ] Сайт пересобран на Netlify с очисткой кэша
- [ ] Кэш браузера очищен
- [ ] Проверены логи Netlify на наличие ошибок
- [ ] Проверена консоль браузера (F12)

### 🔄 После исправлений:

1. Отправьте изменения на GitHub:
   ```bash
   git push origin main
   ```

2. Netlify автоматически пересоберет сайт

3. После успешной сборки проверьте сайт в режиме инкогнито

### 💡 Если проблема сохраняется:

Откройте консоль браузера (F12) и пришлите:
- Ошибки из вкладки **Console**
- Запросы из вкладки **Network** (особенно те, что возвращают ошибки)

Это поможет точно определить проблему.

