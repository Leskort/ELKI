# 🚀 Быстрая инструкция для деплоя на Netlify

## ✅ Что уже готово:

- ✅ `netlify.toml` - конфигурация Netlify
- ✅ `package.json` - все зависимости
- ✅ `.gitignore` - правильные исключения
- ✅ Prisma schema настроен для PostgreSQL

## 📋 Шаги для деплоя:

### 1. Создайте PostgreSQL базу данных

**Рекомендуемые сервисы (бесплатные):**
- **Neon**: https://neon.tech (самый простой)
- **Supabase**: https://supabase.com
- **Railway**: https://railway.app

После создания скопируйте **Connection String** (DATABASE_URL)

### 2. Загрузите код на GitHub

```bash
# Если еще не создали репозиторий на GitHub:
# 1. Создайте новый репозиторий на github.com
# 2. Затем выполните:

git remote add origin https://github.com/ваш-username/название-репозитория.git
git branch -M main
git push -u origin main
```

### 3. Подключите к Netlify

1. Зайдите на https://app.netlify.com
2. Нажмите **"Add new site"** → **"Import an existing project"**
3. Выберите **GitHub** и ваш репозиторий
4. Netlify автоматически определит настройки из `netlify.toml`

### 4. Настройте переменные окружения

В настройках сайта Netlify → **Environment variables** добавьте:

```
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
NEXTAUTH_SECRET=сгенерируйте-случайную-строку-минимум-32-символа
NEXTAUTH_URL=https://ваш-сайт.netlify.app
```

**Как создать NEXTAUTH_SECRET:**
```bash
# В PowerShell:
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

### 5. Выполните миграции базы данных

После первого деплоя нужно создать таблицы в базе данных.

**Вариант 1: Через Netlify CLI (рекомендуется)**
```bash
# Установите Netlify CLI
npm install -g netlify-cli

# Войдите
netlify login

# Подключите к проекту
netlify link

# Выполните миграции (замените YOUR_DATABASE_URL)
$env:DATABASE_URL="YOUR_DATABASE_URL"; npm run db:migrate

# Заполните тестовыми данными
$env:DATABASE_URL="YOUR_DATABASE_URL"; npm run db:seed
```

**Вариант 2: Через Prisma Studio**
```bash
# Запустите Prisma Studio с production базой
$env:DATABASE_URL="YOUR_DATABASE_URL"; npx prisma studio
```

### 6. Проверьте деплой

1. Дождитесь завершения сборки
2. Откройте ваш сайт
3. Проверьте админ-панель: `https://ваш-сайт.netlify.app/login`
4. Войдите: `admin@elki.by` / `admin123`
5. **СРОЧНО измените пароль!**

## ⚠️ Важные моменты:

- ❌ **НЕ загружайте `.env` файл в GitHub** (уже в .gitignore)
- ✅ Используйте **PostgreSQL**, не SQLite
- ✅ `NEXTAUTH_URL` должен быть **полным URL** вашего сайта
- ✅ После изменения переменных окружения **пересоберите сайт**

## 🔧 Если что-то не работает:

1. Проверьте логи сборки в Netlify
2. Убедитесь, что все переменные окружения установлены
3. Проверьте, что база данных доступна извне
4. Убедитесь, что миграции выполнены

## 📞 Полезные ссылки:

- Netlify Docs: https://docs.netlify.com
- Prisma + Netlify: https://www.prisma.io/docs/guides/deployment/deployment-guides/deploying-to-netlify

