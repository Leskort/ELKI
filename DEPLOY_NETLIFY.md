# 🚀 Инструкция по деплою на Netlify

## 📋 Что нужно для деплоя на Netlify

### 1. GitHub репозиторий
- Создайте репозиторий на GitHub
- Загрузите код в репозиторий

### 2. База данных PostgreSQL
Netlify не поддерживает SQLite, нужна PostgreSQL база данных.

**Варианты:**
- **Neon** (бесплатный): https://neon.tech
- **Supabase** (бесплатный): https://supabase.com
- **Railway** (бесплатный): https://railway.app
- **Render** (бесплатный): https://render.com

### 3. Переменные окружения в Netlify

После подключения репозитория к Netlify, добавьте следующие переменные окружения:

```
DATABASE_URL=postgresql://user:password@host:port/database?sslmode=require
NEXTAUTH_SECRET=ваш-секретный-ключ-минимум-32-символа
NEXTAUTH_URL=https://ваш-домен.netlify.app
```

**Как создать NEXTAUTH_SECRET:**
```bash
# В терминале выполните:
openssl rand -base64 32
```

### 4. Настройка Netlify

1. **Подключите репозиторий:**
   - Зайдите на https://app.netlify.com
   - Нажмите "Add new site" → "Import an existing project"
   - Выберите GitHub и ваш репозиторий

2. **Настройки сборки (автоматически из netlify.toml):**
   - Build command: `npm run build`
   - Publish directory: `.next`
   - Node version: `18`

3. **Добавьте переменные окружения:**
   - Site settings → Environment variables
   - Добавьте все переменные из пункта 3

4. **Настройте базу данных:**
   - После создания PostgreSQL базы данных, скопируйте connection string
   - Добавьте его как `DATABASE_URL` в переменные окружения

### 5. Первый деплой

После первого деплоя нужно выполнить миграции базы данных:

1. **Вариант 1: Через Netlify Functions (рекомендуется)**
   - Создайте функцию для выполнения миграций
   - Или используйте Prisma Studio через Netlify CLI

2. **Вариант 2: Локально**
   ```bash
   # Установите Netlify CLI
   npm install -g netlify-cli
   
   # Войдите в Netlify
   netlify login
   
   # Подключите к проекту
   netlify link
   
   # Выполните миграции
   DATABASE_URL="ваш-postgres-url" npm run db:migrate
   
   # Заполните базу тестовыми данными (опционально)
   DATABASE_URL="ваш-postgres-url" npm run db:seed
   ```

### 6. Важные моменты

- ✅ `.env` файл НЕ должен быть в репозитории (уже в .gitignore)
- ✅ Все секреты должны быть в переменных окружения Netlify
- ✅ Используйте PostgreSQL, не SQLite
- ✅ `NEXTAUTH_URL` должен быть полным URL вашего сайта на Netlify
- ✅ После изменения переменных окружения нужно пересобрать сайт

### 7. Проверка после деплоя

1. Проверьте, что сайт открывается
2. Проверьте админ-панель: `https://ваш-домен.netlify.app/login`
3. Войдите с учетными данными по умолчанию
4. Измените пароль администратора

## 🔧 Troubleshooting

**Проблема: Ошибка подключения к базе данных**
- Проверьте `DATABASE_URL` в переменных окружения
- Убедитесь, что база данных доступна извне (не localhost)
- Проверьте SSL режим в connection string

**Проблема: Ошибка авторизации**
- Проверьте `NEXTAUTH_SECRET` и `NEXTAUTH_URL`
- Убедитесь, что `NEXTAUTH_URL` соответствует вашему домену

**Проблема: Ошибка сборки**
- Проверьте логи сборки в Netlify
- Убедитесь, что все зависимости установлены
- Проверьте версию Node.js (должна быть 18)

## 📝 Чек-лист перед деплоем

- [ ] Код загружен в GitHub
- [ ] Создана PostgreSQL база данных
- [ ] Настроены переменные окружения в Netlify
- [ ] Репозиторий подключен к Netlify
- [ ] Выполнены миграции базы данных
- [ ] Сайт успешно собран и задеплоен
- [ ] Проверена работа админ-панели
- [ ] Изменен пароль администратора

