# 🚀 Быстрая настройка Turso (SQLite в облаке)

## Что такое Turso?

Turso - это облачная версия SQLite, которая работает на serverless платформах (включая Netlify). Это идеальное решение для вашего случая!

## ⚡ Быстрая настройка (5 минут)

### 1. Создайте аккаунт Turso

1. Зайдите на https://turso.tech
2. Нажмите "Sign Up" (можно через GitHub)
3. Создайте организацию (organization)

### 2. Создайте базу данных

1. В дашборде Turso нажмите "Create Database"
2. Назовите базу (например, `elki-db`)
3. Выберите регион (ближайший к вам)
4. Нажмите "Create"

### 3. Получите connection string

1. После создания базы, нажмите на неё
2. Перейдите в раздел "Connect"
3. Скопируйте **"Connection String"** (выглядит как `libsql://elki-db-username.turso.io`)

### 4. Создайте токен доступа

1. В дашборде Turso перейдите в "Settings" → "Tokens"
2. Нажмите "Create Token"
3. Назовите токен (например, `netlify-token`)
4. Скопируйте токен (он показывается только один раз!)

### 5. Настройте Netlify

1. Зайдите в настройки вашего сайта на Netlify
2. Перейдите в "Environment variables"
3. Добавьте переменные:

```
DATABASE_URL=libsql://elki-db-username.turso.io?authToken=ваш-токен
NEXTAUTH_SECRET=сгенерируйте-случайную-строку-32-символа
NEXTAUTH_URL=https://ваш-сайт.netlify.app
```

**Важно:** 
- Connection string должен включать токен в формате: `libsql://database-name.turso.io?authToken=ваш-токен`
- Prisma использует провайдер `sqlite`, но Turso connection string работает с ним

### 6. Выполните миграции

После первого деплоя на Netlify, выполните миграции локально:

```bash
# Установите Turso CLI (опционально)
npm install -g @libsql/client

# Или выполните миграции через переменные окружения
DATABASE_URL="libsql://ваш-connection-string" npm run db:migrate
DATABASE_URL="libsql://ваш-connection-string" npm run db:seed
```

Или используйте Turso CLI:

```bash
# Установите Turso CLI
curl -sSfL https://get.tur.so/install.sh | bash

# Войдите
turso auth login

# Выполните миграции
turso db shell elki-db < prisma/migrations/xxx/migration.sql
```

## ✅ Готово!

После этого ваша база данных будет работать на Netlify без PostgreSQL!

## 📊 Преимущества Turso:

- ✅ Бесплатный тариф: 500 MB, 500M rows
- ✅ Работает как SQLite (та же синтаксис)
- ✅ Быстрый и надежный
- ✅ Не требует дополнительных настроек
- ✅ Идеально для serverless

## 🔧 Troubleshooting

**Проблема: Ошибка подключения**
- Проверьте, что токен включен в connection string
- Убедитесь, что токен не истек

**Проблема: Миграции не выполняются**
- Выполните миграции локально с connection string
- Или используйте Turso Studio (веб-интерфейс)

## 📝 Альтернатива: Локальный SQLite для разработки

Для локальной разработки можно использовать обычный SQLite:

```bash
# В .env.local
DATABASE_URL="file:./dev.db"
```

А для production использовать Turso connection string в Netlify.

