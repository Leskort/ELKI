# 🚀 Инструкция по деплою на Netlify

## Подготовка к деплою

### 1. Настройка базы данных

Netlify использует PostgreSQL вместо SQLite. Вам нужно:

1. Создать Netlify Postgres базу данных в панели Netlify
2. Получить connection string из настроек базы данных
3. Обновить `prisma/schema.prisma` для использования PostgreSQL:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

### 2. Переменные окружения в Netlify

В настройках сайта на Netlify добавьте следующие переменные:

- `DATABASE_URL` - Connection string от Netlify Postgres
- `NEXTAUTH_SECRET` - Сгенерируйте случайную строку (минимум 32 символа)
  ```bash
  openssl rand -base64 32
  ```
- `NEXTAUTH_URL` - URL вашего сайта (например: `https://your-site.netlify.app`)

### 3. Деплой через Git

1. Подключите ваш репозиторий к Netlify
2. Настройки билда:
   - **Build command:** `npm run build`
   - **Publish directory:** `.next`
   - **Node version:** 18

### 4. После первого деплоя

Выполните миграции базы данных через Netlify CLI:

```bash
netlify login
netlify link
netlify exec -- npm run db:migrate
```

Или через Netlify Functions можно создать endpoint для миграций.

### 5. Заполнение базы данных

После миграций выполните seed:

```bash
netlify exec -- npm run db:seed
```

## Альтернативный способ (через Netlify Functions)

Можно создать serverless функцию для выполнения миграций:

1. Создайте `netlify/functions/migrate.ts`
2. Добавьте endpoint для выполнения миграций
3. Вызовите его после деплоя

## Проверка работы

После деплоя проверьте:
- ✅ Главная страница открывается
- ✅ Каталог отображает товары
- ✅ Админка доступна по `/admin/login`
- ✅ Можно войти с учетными данными из seed

## Troubleshooting

### Проблема: Prisma Client не найден

Решение: Убедитесь, что `postinstall` скрипт выполняется:
```json
"postinstall": "prisma generate"
```

### Проблема: Ошибки миграций

Решение: Выполните миграции вручную через Netlify CLI или через функцию.

### Проблема: NextAuth не работает

Решение: Проверьте, что `NEXTAUTH_URL` и `NEXTAUTH_SECRET` правильно установлены.

